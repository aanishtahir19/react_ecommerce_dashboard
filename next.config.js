/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['acorn-react-ecommerce-platform.coloredstrategies.com'],
  },
};

module.exports = nextConfig;
